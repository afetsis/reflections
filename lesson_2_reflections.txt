What happens when you initialize a repository? Why do you need to do it?

It creates a .git file that inits the repo.

How is the staging area different from the working directory 
and the repository? What value do you think it offers?

Track the changes and control the items that are going to 
be commited.

How can you use the staging area to make sure you have one commit 
per logical change?

Using git status and git diff (no arguments) i can view diffs 
between working direcotory and staging area.

What are some situations when branches would be helpful in keeping
your history organized? How would branches help?

create different versions, add new experimental features or remove old ones. 